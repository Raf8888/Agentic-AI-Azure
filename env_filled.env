# Environment configuration for agentic_infra_agent
# The following values are placeholders. Replace them with your actual credentials
# Note: Palo Alto firewall variables are left unchanged per user request.

# Azure OpenAI configuration
OPENAI_API_TYPE=azure
OPENAI_API_KEY=
OPENAI_API_BASE=https://<your-openai-resource>.openai.azure.com/
OPENAI_DEPLOYMENT_NAME=<your-deployment-name>

# Palo Alto firewall configuration (unchanged)
PALOALTO_HOST=<firewall-hostname-or-ip>
PALOALTO_USERNAME=<firewall-username>
PALOALTO_PASSWORD=<firewall-password>
